info:
  name: Invalid credential
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/hello?Content-Type=application/json"
  params:
    - name: Content-Type
      value: application/json
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
