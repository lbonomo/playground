info:
  name: Valid credential
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/hello"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
